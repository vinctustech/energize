database {
	driver = "org.postgresql.Driver"
	url = "jdbc:postgresql://localhost:5432/cras"
	username = "postgres"
	password = "postgres"
}

server {
	name = "CRAS"
	version = "0.5"
	port = 8080
	timeout = 15000
}